# ─── Copy this file to .env and fill in real values ─────────────────────────
# .env is gitignored. Never commit it.

# ─── PostgreSQL ───────────────────────────────────────────────────────────────
POSTGRES_DB=postgres
POSTGRES_PASSWORD=your-super-secret-postgres-password

# ─── JWT ─────────────────────────────────────────────────────────────────────
# Must be at least 32 characters. Keep it secret.
JWT_SECRET=your-super-secret-jwt-secret-at-least-32-chars
JWT_EXPIRY=3600

# Generate ANON_KEY and SERVICE_ROLE_KEY at:
#   https://supabase.com/docs/guides/self-hosting/docker#generate-api-keys
# Use the JWT_SECRET above as the signing secret.
ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.<your-anon-key-payload>.<signature>
SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.<your-service-role-payload>.<signature>

# ─── URLs ─────────────────────────────────────────────────────────────────────
SITE_URL=http://localhost:3001
API_EXTERNAL_URL=http://localhost:8000

# ─── Kong ports ───────────────────────────────────────────────────────────────
KONG_HTTP_PORT=8000
KONG_HTTPS_PORT=8443

# ─── Studio port ─────────────────────────────────────────────────────────────
STUDIO_PORT=3001

# ─── Realtime ─────────────────────────────────────────────────────────────────
SECRET_KEY_BASE=your-secret-key-base-at-least-64-characters-long-replace-me

# ─── Auth / Email ─────────────────────────────────────────────────────────────
# GOTRUE_MAILER_AUTOCONFIRM is set to true in docker-compose so these are
# only needed if you want real email delivery locally (e.g. via Mailpit).
GOTRUE_DISABLE_SIGNUP=false
SMTP_ADMIN_EMAIL=admin@localhost
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
