import Foundation

// Copy this file to Constants.swift and fill in your real values.
// Constants.swift is gitignored and will never be committed.

enum Constants {
    enum Supabase {
        static let projectURL = "https://your-project-ref.supabase.co"
        static let anonKey = "your-anon-key-here"
    }

    enum Health {
        static let defaultCalorieGoal = 2000
        static let defaultWaterGoalMl = 2500
    }
}
